---
phase: 02-conversation-chat-ui
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/chat/chat-panel.tsx
  - src/components/chat/message-list.tsx
  - src/components/chat/message-bubble.tsx
  - src/components/chat/typing-indicator.tsx
  - src/components/chat/contextual-pills.tsx
  - src/components/app-shell.tsx
  - src/app/page.tsx
autonomous: true
requirements: [CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-06, CHAT-07, CONV-06]

must_haves:
  truths:
    - "User messages appear right-aligned, AI messages appear left-aligned"
    - "AI responses stream token-by-token as they arrive"
    - "Typing indicator shows immediately on submit and hides when first token arrives"
    - "Message list auto-scrolls to latest message but stops when user scrolls up"
    - "Error state shows inline error message with retry button"
    - "AI messages render markdown (bold, bullets, headers)"
    - "Contextual prompt pills update based on AI suggested_pills metadata"
    - "App transitions from entry screen to chat panel on first message"
  artifacts:
    - path: "src/components/app-shell.tsx"
      provides: "Client-side state machine orchestrating entry/chatting/ready_for_plan phases"
      exports: ["AppShell"]
    - path: "src/components/chat/chat-panel.tsx"
      provides: "Full chat panel with message list, input, and contextual pills"
      exports: ["ChatPanel"]
    - path: "src/components/chat/message-list.tsx"
      provides: "Scrollable message container with auto-scroll behavior"
      exports: ["MessageList"]
    - path: "src/components/chat/message-bubble.tsx"
      provides: "Individual message rendering with role-based styling and markdown"
      exports: ["MessageBubble"]
    - path: "src/components/chat/typing-indicator.tsx"
      provides: "Animated dots typing indicator"
      exports: ["TypingIndicator"]
    - path: "src/components/chat/contextual-pills.tsx"
      provides: "Dynamic pills from AI metadata"
      exports: ["ContextualPills"]
  key_links:
    - from: "src/components/app-shell.tsx"
      to: "@ai-sdk/react"
      via: "useChat hook for message state and streaming"
      pattern: "import.*useChat.*from.*@ai-sdk/react"
    - from: "src/components/app-shell.tsx"
      to: "/api/chat"
      via: "useChat api option"
      pattern: "api.*['\"/]api/chat"
    - from: "src/components/app-shell.tsx"
      to: "src/components/entry/entry-screen.tsx"
      via: "import EntryScreen, render when phase=entry"
      pattern: "import.*EntryScreen"
    - from: "src/components/app-shell.tsx"
      to: "src/components/chat/chat-panel.tsx"
      via: "import ChatPanel, render when phase!=entry"
      pattern: "import.*ChatPanel"
    - from: "src/components/chat/message-bubble.tsx"
      to: "react-markdown"
      via: "import ReactMarkdown for AI message rendering"
      pattern: "import.*[Rr]eact[Mm]arkdown"
---

<objective>
Build the chat panel components (message list, message bubbles, typing indicator, contextual pills) and the AppShell state machine that wires entry screen and chat panel together with the AI SDK useChat hook.

Purpose: Complete the interactive chat experience — users send messages, see streaming AI responses with markdown, and get contextual prompt pills that evolve with the conversation.
Output: Working chat interface with streaming, auto-scroll, error handling, and entry-to-chat transition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-conversation-chat-ui/02-CONTEXT.md
@.planning/phases/02-conversation-chat-ui/02-RESEARCH.md

<interfaces>
<!-- From Plan 01 -->
From src/lib/types.ts:
```typescript
export type AppPhase = 'entry' | 'chatting' | 'ready_for_plan';
export type GatheredInfo = { goal: string | null; background: string | null; constraints: string | null; };
export type ConversationStateData = { gathered_info: GatheredInfo; ready_for_plan: boolean; suggested_pills: string[]; };
export type ChatUIMessage = UIMessage<never, { 'conversation-state': ConversationStateData }>;
```

<!-- From Plan 02 -->
From src/components/entry/entry-screen.tsx:
```typescript
export function EntryScreen({ onSend }: { onSend: (text: string) => void }): JSX.Element;
```

From src/components/chat/chat-input.tsx:
```typescript
export function ChatInput({ onSend, disabled, placeholder }: {
  onSend: (text: string) => void;
  disabled?: boolean;
  placeholder?: string;
}): JSX.Element;
```

From src/components/shared/sparkle-icon.tsx:
```typescript
export function SparkleIcon({ className }: { className?: string }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chat display components (message bubble, message list, typing indicator, contextual pills)</name>
  <files>src/components/chat/message-bubble.tsx, src/components/chat/message-list.tsx, src/components/chat/typing-indicator.tsx, src/components/chat/contextual-pills.tsx</files>
  <read_first>
    - src/lib/types.ts
    - src/components/shared/sparkle-icon.tsx
    - .planning/phases/02-conversation-chat-ui/02-RESEARCH.md (Pattern 2: useChat, Message Bubble with Parts Rendering section, Pitfall 5: auto-scroll)
    - .planning/phases/02-conversation-chat-ui/02-CONTEXT.md (Claude's Discretion items)
  </read_first>
  <action>
1. Create `src/components/chat/message-bubble.tsx` (per CHAT-02, CHAT-07):
   - 'use client' component
   - Props: `message: ChatUIMessage` (import from @/lib/types)
   - Determine role from `message.role`: 'user' or 'assistant'
   - User messages: right-aligned (`justify-end`), blue background (`bg-[#0056d2] text-white`), rounded-2xl, max-w-[80%], px-4 py-3
   - AI messages: left-aligned (`justify-start`), light gray background (`bg-[#f2f5fa] text-gray-900`), rounded-2xl, max-w-[80%], px-4 py-3
   - **Render message.parts array** (v6 API — do NOT use message.content):
     - For `part.type === 'text'`: render plain text for user messages (`<p>`), render with ReactMarkdown for AI messages
     - For data parts (type starts with 'data-'): return null (handled by onData callback, not rendered)
     - For any other part type: return null
   - Import ReactMarkdown from 'react-markdown'. Apply prose styling to markdown container: `className="prose prose-sm max-w-none"`. This handles bold, bullets, headers per CHAT-07.
   - Strip the JSON metadata block from AI text before rendering. Use regex: `/\n*```json\s*\{[\s\S]*?\}\s*```\s*$/` to remove trailing JSON fences from the displayed text. This prevents users from seeing the raw metadata JSON (per Pitfall 2 in RESEARCH.md).

2. Create `src/components/chat/message-list.tsx` (per CHAT-05):
   - 'use client' component
   - Props: `messages: ChatUIMessage[]`, `status: string` (useChat status), `error: Error | undefined`
   - Scrollable container: `overflow-y-auto flex-1 px-4 py-6 space-y-4`
   - **Auto-scroll with manual override** (per Pitfall 5):
     - Use a ref for the scroll container and a sentinel div at the bottom
     - Use IntersectionObserver on the sentinel element
     - When sentinel is visible (user is near bottom): auto-scroll on new messages
     - When sentinel is not visible (user scrolled up): DO NOT auto-scroll
     - Use `useEffect` that watches `messages` array length and `status`. When either changes and sentinel is visible, scroll to bottom with `scrollIntoView({ behavior: 'smooth' })`.
   - Render each message as `<MessageBubble message={msg} key={msg.id} />`
   - Show `<TypingIndicator />` when `status === 'submitted'` (after user sends, before first AI token)
   - **Error display** (per CHAT-06): When `error` is defined, show an inline error card below the last message:
     - Red-tinted background (`bg-red-50 border border-red-200 rounded-lg p-3`)
     - Error text: "Something went wrong. Please try again."
     - "Try again" button (`text-red-600 underline cursor-pointer`) — but the retry action is handled by the parent (ChatPanel), so expose via an `onRetry?: () => void` prop

3. Create `src/components/chat/typing-indicator.tsx` (per CHAT-04):
   - 'use client' component
   - No props needed (or optional `className`)
   - Renders 3 animated dots in a left-aligned bubble (same style as AI message bubble)
   - Animation: dots bounce with staggered delays using Tailwind `animate-bounce` with custom animation-delay via inline styles:
     - Dot 1: delay 0ms
     - Dot 2: delay 150ms
     - Dot 3: delay 300ms
   - Each dot: `w-2 h-2 rounded-full bg-gray-400`
   - Container matches AI message style: `bg-[#f2f5fa] rounded-2xl px-4 py-3 inline-flex gap-1 items-center`

4. Create `src/components/chat/contextual-pills.tsx` (per CONV-06):
   - 'use client' component
   - Props: `pills: string[]`, `onSelect: (text: string) => void`, `disabled?: boolean`
   - Renders a horizontal scrolling row of pills (similar to PromptPillRow but more compact for the chat context)
   - Each pill is a small button with SparkleIcon + text
   - Styling: `flex gap-2 overflow-x-auto py-2 scrollbar-hide`
   - Pills: `whitespace-nowrap rounded-full border border-[#dae1ed] bg-white px-3 py-1.5 text-xs hover:bg-[#f2f5fa] transition-colors flex items-center gap-1.5`
   - Disabled state: reduced opacity, pointer-events-none
   - When empty pills array: render nothing (return null)
  </action>
  <verify>
    <automated>cd /Users/dajiboye/base/coursera/ppp-prototype && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/chat/message-bubble.tsx` exports `MessageBubble`, imports ReactMarkdown, renders `message.parts` (NOT message.content)
    - MessageBubble contains JSON metadata stripping regex
    - MessageBubble has `bg-[#0056d2]` for user and `bg-[#f2f5fa]` for AI
    - `src/components/chat/message-list.tsx` exports `MessageList`, uses IntersectionObserver for auto-scroll
    - MessageList renders TypingIndicator when status is 'submitted'
    - MessageList shows error card with "Try again" when error prop is defined
    - `src/components/chat/typing-indicator.tsx` exports `TypingIndicator` with 3 animated dots
    - `src/components/chat/contextual-pills.tsx` exports `ContextualPills`, imports SparkleIcon
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Message bubble renders user/AI messages with markdown, message list auto-scrolls with manual override, typing indicator shows animated dots, contextual pills display dynamic AI suggestions</done>
</task>

<task type="auto">
  <name>Task 2: Create ChatPanel, AppShell state machine, and wire page.tsx</name>
  <files>src/components/chat/chat-panel.tsx, src/components/app-shell.tsx, src/app/page.tsx</files>
  <read_first>
    - src/lib/types.ts
    - src/components/chat/message-list.tsx (just created)
    - src/components/chat/chat-input.tsx (from Plan 02)
    - src/components/chat/contextual-pills.tsx (just created)
    - src/components/entry/entry-screen.tsx (from Plan 02)
    - src/app/page.tsx
    - .planning/phases/02-conversation-chat-ui/02-RESEARCH.md (Pattern 2: useChat, Pattern 3: CSS Grid Transition, Anti-Patterns)
    - .planning/phases/02-conversation-chat-ui/02-CONTEXT.md (D-02, D-05, D-06)
  </read_first>
  <action>
1. Create `src/components/chat/chat-panel.tsx`:
   - 'use client' component
   - Props: `messages: ChatUIMessage[]`, `status: string`, `error: Error | undefined`, `suggestedPills: string[]`, `onSend: (text: string) => void`, `onRetry: () => void`
   - Layout: flex flex-col h-full
   - Top: `<MessageList messages={messages} status={status} error={error} onRetry={onRetry} />` (takes flex-1)
   - Bottom section (sticky at bottom):
     - `<ContextualPills pills={suggestedPills} onSelect={onSend} disabled={status === 'submitted' || status === 'streaming'} />`
     - `<ChatInput onSend={onSend} disabled={status === 'submitted' || status === 'streaming'} />`
   - Bottom section has a subtle top border (`border-t border-[#dae1ed]`) and padding (`px-4 py-3`)
   - Max-width container for messages area (~768px, centered) to keep messages readable

2. Create `src/components/app-shell.tsx` (per D-02, D-05, D-06):
   - 'use client' component — this is the main orchestrator
   - State:
     - `phase: AppPhase` — starts as 'entry', managed with useState
     - `suggestedPills: string[]` — pills from AI metadata, starts empty
     - `gatheredInfo: GatheredInfo` — accumulated from AI metadata
   - **useChat hook** (per D-02, D-05 — from '@ai-sdk/react'):
     ```typescript
     const { messages, sendMessage, status, error } = useChat({
       api: '/api/chat',
     });
     ```
     Note: The exact useChat API in v6 may vary. If `useChat` doesn't accept a generic type parameter, adapt accordingly. The key is to use useChat from '@ai-sdk/react' with `api: '/api/chat'`.

   - **Extract metadata from messages:** After each message update, scan the latest assistant message's parts for conversation-state data parts. If found, update suggestedPills and gatheredInfo state. If `ready_for_plan` is true, set phase to 'ready_for_plan'.

     If the useChat hook provides an `onData` or `onToolCall` callback, use that. Otherwise, use a `useEffect` that watches `messages` and extracts data parts from the latest assistant message. As a fallback, parse the JSON metadata block from the last assistant message text using the same regex pattern as the route handler.

   - **handleSend callback:**
     - If phase is 'entry', set phase to 'chatting'
     - Call `sendMessage({ text })` (v6 API) or `sendMessage(text)` — adapt to actual installed API
     - Wrapped in useCallback with [phase, sendMessage] deps

   - **handleRetry callback:** Re-send the last user message (get from messages array, filter role=user, take last)

   - **Layout and transition** (per D-06):
     - Full-screen container: `h-screen transition-all duration-500 ease-out`
     - When `phase === 'entry'`: render EntryScreen centered (`flex items-center justify-center`)
     - When `phase !== 'entry'`: render ChatPanel in a flex column layout
     - CSS transition handles the switch smoothly

   - Pass to EntryScreen: `onSend={handleSend}`
   - Pass to ChatPanel: `messages, status, error, suggestedPills, onSend: handleSend, onRetry: handleRetry`

3. Update `src/app/page.tsx`:
   - Replace the placeholder content with `<AppShell />`
   - Import AppShell from '@/components/app-shell'
   - Keep it as a server component that renders the client component:
     ```typescript
     import { AppShell } from '@/components/app-shell';

     export default function Home() {
       return <AppShell />;
     }
     ```
  </action>
  <verify>
    <automated>cd /Users/dajiboye/base/coursera/ppp-prototype && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/chat/chat-panel.tsx` exports `ChatPanel`, imports MessageList, ChatInput, ContextualPills
    - `src/components/app-shell.tsx` exports `AppShell`, has 'use client' directive
    - AppShell imports `useChat` from `@ai-sdk/react`
    - AppShell contains `api: '/api/chat'` in useChat config
    - AppShell imports and renders EntryScreen when phase is 'entry'
    - AppShell imports and renders ChatPanel when phase is not 'entry'
    - AppShell contains `useState<AppPhase>('entry')` or equivalent phase state
    - AppShell contains `duration-500` for CSS transition timing
    - `src/app/page.tsx` imports and renders AppShell
    - TypeScript compilation passes
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>ChatPanel composes all chat components, AppShell orchestrates entry/chat transition with useChat hook, page.tsx renders the app. Full chat flow works: entry screen -> send message -> streaming AI response with typing indicator, auto-scroll, markdown, and contextual pills.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. All 7 component files exist
4. AppShell uses useChat from @ai-sdk/react
5. Messages render via parts array (not content string)
6. Auto-scroll uses IntersectionObserver
7. Entry-to-chat transition uses CSS transition
8. Contextual pills render from AI metadata
</verification>

<success_criteria>
- Full chat flow works end-to-end: entry screen -> first message -> streaming AI response
- User messages right-aligned (blue), AI messages left-aligned (gray) with markdown rendering
- Typing indicator shows during 'submitted' status
- Auto-scroll works but respects manual scroll-up
- Error state shows inline with retry button
- Contextual pills update from AI metadata
- Entry-to-chat CSS transition is smooth (duration-500, ease-out)
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-conversation-chat-ui/02-03-SUMMARY.md`
</output>
