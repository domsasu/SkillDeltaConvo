---
phase: 02-conversation-chat-ui
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/entry/entry-screen.tsx
  - src/components/entry/prompt-pill-row.tsx
  - src/components/entry/prompt-pill.tsx
  - src/components/entry/suggestion-buttons.tsx
  - src/components/chat/chat-input.tsx
autonomous: true
requirements: [ENTRY-01, ENTRY-02, ENTRY-03, ENTRY-04, ENTRY-05, CHAT-01]

must_haves:
  truths:
    - "Entry screen shows centered greeting text with personalized message"
    - "Chat input box is centered below greeting with placeholder 'I want to learn...'"
    - "Three rows of horizontally scrolling prompt pills are visible with AI sparkle icons"
    - "Prompt pill rows have gradient fade at edges"
    - "Row of suggestion buttons appears below input"
    - "Background has soft blue-purple gradient"
    - "Clicking a prompt pill or suggestion button calls the onSend callback with the pill/button text"
  artifacts:
    - path: "src/components/entry/entry-screen.tsx"
      provides: "Full entry screen layout with greeting, input, pills, suggestions"
      exports: ["EntryScreen"]
    - path: "src/components/entry/prompt-pill-row.tsx"
      provides: "Horizontally scrolling row of pills with gradient fade"
      exports: ["PromptPillRow"]
    - path: "src/components/entry/prompt-pill.tsx"
      provides: "Individual pill button with sparkle icon"
      exports: ["PromptPill"]
    - path: "src/components/entry/suggestion-buttons.tsx"
      provides: "Row of suggestion buttons with sparkle icons"
      exports: ["SuggestionButtons"]
    - path: "src/components/chat/chat-input.tsx"
      provides: "Auto-resize textarea with send button"
      exports: ["ChatInput"]
  key_links:
    - from: "src/components/entry/entry-screen.tsx"
      to: "src/components/entry/prompt-pill-row.tsx"
      via: "import and render 3 PromptPillRow instances"
      pattern: "PromptPillRow"
    - from: "src/components/entry/entry-screen.tsx"
      to: "src/components/chat/chat-input.tsx"
      via: "import ChatInput for centered input box"
      pattern: "ChatInput"
    - from: "src/components/entry/prompt-pill.tsx"
      to: "src/components/shared/sparkle-icon.tsx"
      via: "import SparkleIcon"
      pattern: "SparkleIcon"
---

<objective>
Build the entry screen that greets learners with a welcoming message, scrolling prompt pills for quick conversation starters, and suggestion buttons. Also build the shared ChatInput component used by both entry screen and chat panel.

Purpose: Create the first thing learners see — an inviting, low-friction entry point to start a conversation about their career goals.
Output: EntryScreen component, PromptPillRow, PromptPill, SuggestionButtons, ChatInput components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-conversation-chat-ui/02-CONTEXT.md
@.planning/phases/02-conversation-chat-ui/02-RESEARCH.md

<interfaces>
<!-- From Plan 01 (types created there) -->
From src/lib/types.ts:
```typescript
export type AppPhase = 'entry' | 'chatting' | 'ready_for_plan';
```

From src/components/shared/sparkle-icon.tsx:
```typescript
export function SparkleIcon({ className }: { className?: string }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChatInput component and prompt pill components</name>
  <files>src/components/chat/chat-input.tsx, src/components/entry/prompt-pill.tsx, src/components/entry/prompt-pill-row.tsx, src/components/entry/suggestion-buttons.tsx</files>
  <read_first>
    - src/lib/types.ts
    - src/components/shared/sparkle-icon.tsx
    - src/app/globals.css
    - .planning/phases/02-conversation-chat-ui/02-CONTEXT.md (specifics section for pill content, gradient values)
    - .planning/phases/02-conversation-chat-ui/02-RESEARCH.md (Prompt Pill Row with Gradient Fade section, Pitfall 4)
  </read_first>
  <action>
1. Create `src/components/chat/chat-input.tsx` (per CHAT-01):
   - 'use client' component
   - Props: `onSend: (text: string) => void`, `disabled?: boolean`, `placeholder?: string` (default "I want to learn...")
   - Auto-resize textarea using CSS `field-sizing: content` (modern browsers, good enough for desktop prototype). Set `min-rows` via min-height (2 rows) and max-height (6 rows).
   - Send button on the right side (use Send icon from lucide-react). Button is disabled when textarea is empty or `disabled` prop is true.
   - Submit on Enter key (without Shift). Shift+Enter inserts newline.
   - On submit: call `onSend(text)`, clear the textarea.
   - Styling: bordered box with rounded-2xl, border color #dae1ed, focus ring with primary blue #0056d2. Textarea has no resize handle. Send button is a circle with bg-[#0056d2] text-white, 36px.
   - Include a decorative microphone icon (Mic from lucide-react) on the left as a non-functional visual element matching the Figma design.

2. Create `src/components/entry/prompt-pill.tsx`:
   - 'use client' component
   - Props: `text: string`, `onClick: () => void`
   - Renders a button with SparkleIcon (from shared/sparkle-icon.tsx) + text
   - Styling: `whitespace-nowrap rounded-full border border-[#dae1ed] bg-white px-4 py-2 text-sm hover:bg-[#f2f5fa] transition-colors flex items-center gap-2`
   - 32px height (use h-8)

3. Create `src/components/entry/prompt-pill-row.tsx` (per ENTRY-03):
   - 'use client' component
   - Props: `pills: string[]`, `onSelect: (text: string) => void`
   - Horizontal scroll container with `overflow-x-auto` and `scrollbar-hide` (add CSS utility if needed in globals.css)
   - Gradient fade at edges using CSS mask-image:
     ```
     maskImage: 'linear-gradient(to right, transparent, black 5%, black 95%, transparent)'
     WebkitMaskImage: 'linear-gradient(to right, transparent, black 5%, black 95%, transparent)'
     ```
   - Renders array of PromptPill components in a flex row with gap-3
   - Add `scrollbar-hide` utility to `globals.css` if not present: `.scrollbar-hide::-webkit-scrollbar { display: none; } .scrollbar-hide { -ms-overflow-style: none; scrollbar-width: none; }`

4. Create `src/components/entry/suggestion-buttons.tsx` (per ENTRY-04):
   - 'use client' component
   - Props: `onSelect: (text: string) => void`
   - Renders 4 hardcoded suggestion buttons in a flex-wrap row:
     - "Create a learning plan"
     - "Find a new career"
     - "Develop in-demand skills"
     - "Advance my career"
   - Each button has SparkleIcon + text
   - Styling: `rounded-lg border border-[#dae1ed] bg-white px-4 py-2.5 text-sm hover:bg-[#f2f5fa] transition-colors flex items-center gap-2`
  </action>
  <verify>
    <automated>cd /Users/dajiboye/base/coursera/ppp-prototype && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/chat/chat-input.tsx` exports `ChatInput` with `onSend` prop
    - ChatInput contains "I want to learn" as default placeholder
    - ChatInput handles Enter key submit (grep for "Enter" in file)
    - `src/components/entry/prompt-pill.tsx` exports `PromptPill` and imports `SparkleIcon`
    - `src/components/entry/prompt-pill-row.tsx` exports `PromptPillRow` with maskImage gradient fade
    - `src/components/entry/suggestion-buttons.tsx` exports `SuggestionButtons` with 4 button texts: "Create a learning plan", "Find a new career", "Develop in-demand skills", "Advance my career"
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>ChatInput, PromptPill, PromptPillRow, and SuggestionButtons components created with correct styling and interactivity</done>
</task>

<task type="auto">
  <name>Task 2: Create EntryScreen component with full layout</name>
  <files>src/components/entry/entry-screen.tsx</files>
  <read_first>
    - src/components/chat/chat-input.tsx (just created)
    - src/components/entry/prompt-pill-row.tsx (just created)
    - src/components/entry/suggestion-buttons.tsx (just created)
    - .planning/phases/02-conversation-chat-ui/02-CONTEXT.md (specifics section — pill content per row, gradient background value)
  </read_first>
  <action>
Create `src/components/entry/entry-screen.tsx` (per ENTRY-01, ENTRY-02, ENTRY-03, ENTRY-04, ENTRY-05):

- 'use client' component
- Props: `onSend: (text: string) => void`
- Layout: Centered vertically and horizontally, max-width ~700px for the content area
- Background: Apply the soft blue-purple gradient from CONTEXT.md specifics:
  `background: linear-gradient(90deg, #fff 0%, rgba(53,135,252,0.1) 33%, rgba(164,154,255,0.05) 67%, #fff 100%)`
  Apply to a full-screen wrapper div.

- Components arranged top-to-bottom:
  1. **Greeting text** (per ENTRY-01): "Hello! I can recommend courses that fit your goals." on one line, "What do you want to learn and for what role?" on the next. Use text-2xl or text-3xl, font-semibold, text-center, text-gray-900. Add a small sparkle icon or decorative element.

  2. **ChatInput** centered below greeting (per ENTRY-02): Render `<ChatInput onSend={onSend} />` with some top margin (mt-6).

  3. **Three PromptPillRow instances** (per ENTRY-03): Below the input with mt-8. Each row gets a different set of pills:
     - Row 1 (career change): ["I want to switch careers into technology", "Figma for UI/UX designers", "Start my career as a data analyst", "Transition to product management"]
     - Row 2 (skills): ["AI tools for my workflow", "SQL for data analysts", "Python for building apps", "Learn cloud computing", "Digital marketing fundamentals"]
     - Row 3 (mixed): ["Help me decide what to learn", "I want to earn a certificate", "Prepare for a job interview", "Build a portfolio project"]

  4. **SuggestionButtons** below pills (per ENTRY-04): Render `<SuggestionButtons onSelect={onSend} />` with mt-6.

- When a pill or suggestion button is clicked, it calls `onSend(text)` which the parent (app-shell in Plan 03) will use to send the first message and transition to chat phase.
  </action>
  <verify>
    <automated>cd /Users/dajiboye/base/coursera/ppp-prototype && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/entry/entry-screen.tsx` exports `EntryScreen` component
    - File contains the greeting text "I can recommend courses that fit your goals"
    - File contains the gradient background value "rgba(53,135,252,0.1)"
    - File imports and renders ChatInput, PromptPillRow (3 instances), and SuggestionButtons
    - File contains at least 3 arrays of pill strings (one per row)
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>EntryScreen shows greeting, centered chat input, 3 rows of scrolling prompt pills with gradient fade, suggestion buttons, and blue-purple gradient background</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. All 5 component files exist in src/components/
3. EntryScreen imports and composes all sub-components
4. Prompt pills include sparkle icons
5. Gradient fade on pill rows uses maskImage
6. Background gradient uses the exact CONTEXT.md values
</verification>

<success_criteria>
- Entry screen renders greeting, input, 3 pill rows, and suggestion buttons per Figma design
- ChatInput handles auto-resize, Enter submit, and send button
- Prompt pills scroll horizontally with edge gradient fade
- All components are typed and compile without errors
- Components accept callbacks for parent wiring (Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/02-conversation-chat-ui/02-02-SUMMARY.md`
</output>
