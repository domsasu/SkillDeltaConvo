---
phase: 03.1-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shared/sparkle-icon.tsx
  - src/lib/types.ts
  - src/lib/prompts/schemas.ts
  - src/lib/prompts/system-prompt.ts
  - src/app/api/chat/route.ts
  - src/app/globals.css
  - src/components/lihp/learning-plan-banner.tsx
  - src/components/shared/header.tsx
  - src/components/lihp/chat-side-panel.tsx
  - src/components/entry/prompt-pill.tsx
  - src/components/entry/suggestion-buttons.tsx
autonomous: true
requirements:
  - UXP-01
  - UXP-05

must_haves:
  truths:
    - "Sparkle icons use brand SVGs with radial gradient (purple-to-blue), not lucide Sparkles"
    - "Page headers (LIHP chat panel header, shared header) use the open 4-point star variant"
    - "Pills, plan banner use the two-star (brand_default) variant"
    - "LLM responses are concise (2-3 sentences per conversational turn)"
    - "Structured pills schema supports both string[] and {type, question, options} union"
  artifacts:
    - path: "src/components/shared/sparkle-icon.tsx"
      provides: "SparkleIcon (brand_default) and SparkleOpenIcon (brand_open) components"
      exports: ["SparkleIcon", "SparkleOpenIcon"]
    - path: "src/lib/types.ts"
      provides: "Updated ConversationStateData with StructuredPillData union type"
      contains: "StructuredPillData"
    - path: "src/lib/prompts/schemas.ts"
      provides: "Updated Zod schema supporting structured pills union"
      contains: "structuredPillSchema"
    - path: "src/app/globals.css"
      provides: "pulseOpacity keyframe for progressive module animation"
      contains: "pulseOpacity"
  key_links:
    - from: "src/components/shared/sparkle-icon.tsx"
      to: "sparkle/_genAI_sparkle_brand_default.svg"
      via: "inline SVG markup"
      pattern: "paint0_radial"
    - from: "src/components/lihp/chat-side-panel.tsx"
      to: "src/components/shared/sparkle-icon.tsx"
      via: "import SparkleOpenIcon"
      pattern: "SparkleOpenIcon"
---

<objective>
Replace sparkle icons with brand SVGs, update schemas for structured pills, make LLM responses concise, and add CSS keyframe for progressive module.

Purpose: Foundation changes that all other Phase 3.1 plans depend on — types, schemas, icons, system prompt, CSS.
Output: Updated sparkle components, schema with structured pill union, concise system prompt, pulseOpacity keyframe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ux-polish/03.1-CONTEXT.md
@.planning/phases/03.1-ux-polish/03.1-UI-SPEC.md

<interfaces>
<!-- Current SparkleIcon used across 6 files — all imports come from shared/sparkle-icon.tsx -->
From src/components/shared/sparkle-icon.tsx:
```typescript
export function SparkleIcon({ className }: { className?: string })
// Currently uses lucide Sparkles — will be replaced with brand SVG
```

From src/lib/types.ts:
```typescript
export type ConversationStateData = {
  gathered_info: GatheredInfo;
  ready_for_plan: boolean;
  suggested_pills: string[];  // Will become string[] | StructuredPillData
};
```

From src/lib/prompts/schemas.ts:
```typescript
export const conversationStateSchema = z.object({
  gathered_info: gatheredInfoSchema,
  ready_for_plan: z.boolean(),
  suggested_pills: z.array(z.string()).min(2).max(5),  // Will become union
});
```

Files importing SparkleIcon (all need import update for open variant where applicable):
- src/components/lihp/learning-plan-banner.tsx (uses SparkleIcon — keep brand_default)
- src/components/lihp/chat-side-panel.tsx (uses SparkleIcon — change to SparkleOpenIcon per D-01)
- src/components/shared/header.tsx (uses SparkleIcon — change to SparkleOpenIcon per D-01)
- src/components/entry/prompt-pill.tsx (uses SparkleIcon — keep brand_default)
- src/components/entry/suggestion-buttons.tsx (uses SparkleIcon — keep brand_default)
- src/components/chat/contextual-pills.tsx (uses SparkleIcon — keep brand_default)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace sparkle icons with brand SVGs and update all import sites</name>
  <files>
    src/components/shared/sparkle-icon.tsx,
    src/components/lihp/chat-side-panel.tsx,
    src/components/shared/header.tsx,
    src/components/lihp/learning-plan-banner.tsx,
    src/components/entry/prompt-pill.tsx,
    src/components/entry/suggestion-buttons.tsx,
    src/components/chat/contextual-pills.tsx
  </files>
  <read_first>
    sparkle/_genAI_sparkle_brand_default.svg,
    sparkle/_genAI_sparkle_brand_open.svg,
    src/components/shared/sparkle-icon.tsx,
    src/components/lihp/chat-side-panel.tsx,
    src/components/shared/header.tsx,
    src/components/lihp/learning-plan-banner.tsx,
    src/components/entry/prompt-pill.tsx,
    src/components/entry/suggestion-buttons.tsx,
    src/components/chat/contextual-pills.tsx
  </read_first>
  <action>
Per D-01 and D-02, replace the sparkle-icon.tsx file with two exported components:

1. **SparkleIcon** — Renders `_genAI_sparkle_brand_default.svg` inline as JSX. Two-star design (large 4-point + small 4-point) with radial gradient `#6923DE` to `#3286FF`. Default size 22x22px. Accepts `className` prop for sizing. Use unique gradient IDs (prefix with `sparkle-default-`) to avoid SVG ID collisions when multiple instances render.

2. **SparkleOpenIcon** — Renders `_genAI_sparkle_brand_open.svg` inline as JSX. Open 4-point star with radial gradient `#6923DE` to `#3286FF`, white/light center detail (`#F2F5FA`). Default size 21x22px (matching SVG viewBox). Accepts `className` prop. Use unique gradient IDs (prefix with `sparkle-open-`).

Remove the `lucide-react` Sparkles import entirely from this file.

Then update import sites per D-01:
- **chat-side-panel.tsx**: Change `import { SparkleIcon }` to `import { SparkleOpenIcon }` and use `<SparkleOpenIcon>` in the panel header (page header context = brand_open).
- **header.tsx**: Change `import { SparkleIcon }` to `import { SparkleOpenIcon }` and use `<SparkleOpenIcon>` (page header context = brand_open).
- **learning-plan-banner.tsx**: Keep `SparkleIcon` import (plan module context = brand_default). No change needed.
- **prompt-pill.tsx**: Keep `SparkleIcon` import (pill context = brand_default). No change needed.
- **suggestion-buttons.tsx**: Keep `SparkleIcon` import if it uses one. No change needed.
- **contextual-pills.tsx**: Keep `SparkleIcon` import (pill context = brand_default). No change needed.
  </action>
  <verify>
    <automated>cd /Users/dajiboye/base/coursera/ppp-prototype && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - sparkle-icon.tsx exports both `SparkleIcon` and `SparkleOpenIcon`
    - sparkle-icon.tsx does NOT import from `lucide-react`
    - sparkle-icon.tsx contains `radialGradient` elements with stops `#6923DE` and `#3286FF`
    - chat-side-panel.tsx imports `SparkleOpenIcon` (not `SparkleIcon`)
    - header.tsx imports `SparkleOpenIcon` (not `SparkleIcon`)
    - learning-plan-banner.tsx still imports `SparkleIcon` (not SparkleOpenIcon)
    - Build passes with no errors
  </acceptance_criteria>
  <done>Both sparkle SVG variants render as inline React components with gradient fills. Header contexts use SparkleOpenIcon, pill/module contexts use SparkleIcon. No lucide Sparkles dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Update types, schemas, system prompt, CSS keyframe, and route handler for structured pills and conciseness</name>
  <files>
    src/lib/types.ts,
    src/lib/prompts/schemas.ts,
    src/lib/prompts/system-prompt.ts,
    src/app/api/chat/route.ts,
    src/app/globals.css
  </files>
  <read_first>
    src/lib/types.ts,
    src/lib/prompts/schemas.ts,
    src/lib/prompts/system-prompt.ts,
    src/app/api/chat/route.ts,
    src/app/globals.css
  </read_first>
  <action>
**types.ts** — Per D-11, add a `StructuredPillData` type and update `ConversationStateData`:
```typescript
export type StructuredPillData = {
  type: "multi" | "single";
  question: string;
  options: string[];
};

export type ConversationStateData = {
  gathered_info: GatheredInfo;
  ready_for_plan: boolean;
  suggested_pills: string[] | StructuredPillData;
};
```

**schemas.ts** — Update the `conversationStateSchema` to support the union. Use `z.union()`:
```typescript
export const structuredPillSchema = z.object({
  type: z.enum(["multi", "single"]),
  question: z.string(),
  options: z.array(z.string()).min(2).max(20),
});

export const conversationStateSchema = z.object({
  gathered_info: gatheredInfoSchema,
  ready_for_plan: z.boolean(),
  suggested_pills: z.union([
    z.array(z.string()).min(2).max(5),
    structuredPillSchema,
  ]),
});
```

**system-prompt.ts** — Per D-17, add conciseness directive. Insert after the "## Conversation Guidelines" section, update the bullet about keeping responses concise:
- Change the existing "Keep responses concise -- 2-4 sentences" bullet to: "Keep your responses concise -- 2-3 sentences maximum for each conversational turn. Be direct and friendly. Do not provide long explanations or bulleted lists unless the user specifically asks for details."

Also update the "## Suggested Pills Guidelines" section to instruct the AI about structured pill format. Add a new subsection after the current pill guidelines:

```
## Structured Options

When asking the user to choose from a set of predefined options, use the structured pill format instead of string pills:

**Multichoice** (user picks 1 or more):
- suggested_pills: { "type": "multi", "question": "What roles interest you?", "options": ["Data Analyst", "Data Scientist", "ML Engineer", "Business Analyst", "Data Engineer", "Product Analyst"] }

**Single-choice** (user picks exactly 1):
- suggested_pills: { "type": "single", "question": "How much time can you dedicate per week?", "options": ["3-5 hours", "5-10 hours", "10-15 hours", "15+ hours"] }

Use structured pills for the FIRST question about career goals (multichoice with role options) and the constraints question (single-choice with time options). Use regular string pills for follow-up and refinement prompts.
```

**route.ts** — Update the `DEFAULT_STATE` type to remain compatible (it uses `string[]` which is still valid in the union). Update the type annotation on `conversationState` extraction to use the new schema. The `extractMetadataFromText` function already uses `conversationStateSchema.safeParse` so it will auto-support the new union. No structural changes needed — just verify the `DEFAULT_STATE` constant still type-checks.

**globals.css** — Add the `pulseOpacity` keyframe (per UI spec) before the `prefers-reduced-motion` media query:
```css
@keyframes pulseOpacity {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.4; }
}
```
  </action>
  <verify>
    <automated>cd /Users/dajiboye/base/coursera/ppp-prototype && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains `StructuredPillData` type with `type: "multi" | "single"`
    - types.ts ConversationStateData has `suggested_pills: string[] | StructuredPillData`
    - schemas.ts exports `structuredPillSchema`
    - schemas.ts conversationStateSchema uses `z.union`
    - system-prompt.ts contains "2-3 sentences maximum"
    - system-prompt.ts contains "Structured Options" section
    - globals.css contains `@keyframes pulseOpacity`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Types support structured pill union, Zod schemas validate both formats, system prompt enforces 2-3 sentence conciseness and instructs structured pill usage, pulseOpacity keyframe available for progressive module.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `grep -r "lucide-react" src/components/shared/sparkle-icon.tsx` returns nothing
- `grep "SparkleOpenIcon" src/components/lihp/chat-side-panel.tsx src/components/shared/header.tsx` returns matches in both files
- `grep "StructuredPillData" src/lib/types.ts` returns match
- `grep "structuredPillSchema" src/lib/prompts/schemas.ts` returns match
- `grep "2-3 sentences maximum" src/lib/prompts/system-prompt.ts` returns match
- `grep "pulseOpacity" src/app/globals.css` returns match
</verification>

<success_criteria>
Brand sparkle SVGs render in all icon locations with correct variant assignment (open for headers, default for pills/module). Type system supports structured pill data. System prompt enforces concise responses. CSS keyframe ready for progressive module animation. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ux-polish/03.1-01-SUMMARY.md`
</output>
