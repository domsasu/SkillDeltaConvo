---
phase: 03-plan-generation-display
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/plan/plan-view.tsx
  - src/components/plan/plan-summary-bar.tsx
  - src/components/plan/milestone-section.tsx
  - src/components/plan/plan-course-card.tsx
  - src/components/plan/activity-badge.tsx
  - src/components/plan/plan-loading-skeleton.tsx
autonomous: true
requirements: [DISP-02, DISP-03, DISP-04, DISP-05]

must_haves:
  truths:
    - "Full plan view shows plan title, Start CTA, summary bar, and milestone sections"
    - "Each milestone section contains course cards with name, partner, duration, skills, and thumbnail"
    - "Course names link to coursera.org in a new tab"
    - "Plan summary bar shows role, key skills, duration, and hours/week"
    - "Loading skeleton mimics plan layout with pulsing placeholders"
  artifacts:
    - path: "src/components/plan/plan-view.tsx"
      provides: "Full plan view container with back link, title, CTA, summary bar, milestones"
      exports: ["PlanView"]
    - path: "src/components/plan/plan-summary-bar.tsx"
      provides: "Summary line with role, skills, duration, hours"
      exports: ["PlanSummaryBar"]
    - path: "src/components/plan/milestone-section.tsx"
      provides: "Milestone container with name, description, skills, badges, course cards"
      exports: ["MilestoneSection"]
    - path: "src/components/plan/plan-course-card.tsx"
      provides: "Course card with thumbnail, name link, skills, partner, badges"
      exports: ["PlanCourseCard"]
    - path: "src/components/plan/activity-badge.tsx"
      provides: "Mocked activity badge pill"
      exports: ["ActivityBadge"]
    - path: "src/components/plan/plan-loading-skeleton.tsx"
      provides: "Skeleton placeholder during plan generation"
      exports: ["PlanLoadingSkeleton"]
  key_links:
    - from: "src/components/plan/plan-view.tsx"
      to: "src/components/plan/milestone-section.tsx"
      via: "renders MilestoneSection for each milestone"
      pattern: "MilestoneSection"
    - from: "src/components/plan/milestone-section.tsx"
      to: "src/components/plan/plan-course-card.tsx"
      via: "renders PlanCourseCard for each course"
      pattern: "PlanCourseCard"
    - from: "src/components/plan/plan-course-card.tsx"
      to: "coursera.org"
      via: "anchor tag with target=_blank on course name"
      pattern: "target=\"_blank\""
---

<objective>
Plan display components: PlanView, MilestoneSection, PlanCourseCard, PlanSummaryBar, ActivityBadge, PlanLoadingSkeleton

Purpose: Build all the UI components needed to render a learning plan with milestones and course cards, matching the Figma design. These are pure presentational components that receive plan data as props.

Output: Six new components in `src/components/plan/` that compose into the full plan view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-plan-generation-display/03-CONTEXT.md
@.planning/phases/03-plan-generation-display/03-UI-SPEC.md
@.planning/phases/03-plan-generation-display/03-01-SUMMARY.md

@src/lib/plan-types.ts
@src/components/lihp/learning-plan-banner.tsx
@src/components/lihp/course-card.tsx

<interfaces>
<!-- Types from Plan 01 that these components consume -->

From src/lib/plan-types.ts (created in 03-01):
```typescript
export type PlanCourse = {
  id: string;
  name: string;
  url: string;
  imageUrl: string;
  productType: string;
  partners: string[];
  skills: string[];
  duration: string;
  productDifficultyLevel: string;
  estimatedHours: number;
};

export type PlanMilestone = {
  id: string;
  name: string;
  description: string;
  skills: string[];
  badges: string[];
  courses: PlanCourse[];
  estimatedWeeks: number;
};

export type LearningPlan = {
  title: string;
  summary: {
    role: string;
    skills: string[];
    totalDuration: string;
    hoursPerWeek: string;
  };
  milestones: PlanMilestone[];
};
```

Existing design tokens (from UI-SPEC and codebase):
- text-primary: `#0f1114` (headings, course names, bold spans)
- text-secondary: `#5b6780` (partner names, skills, metadata)
- border: `#dae1ed` (card borders, dividers)
- accent: `#0056d2` (CTAs, links, badge text)
- accent-hover: `#0048b0` (CTA hover)
- accent-surface: `#e3eeff` (badge backgrounds)
- skeleton-pulse: `#f2f5fa` (loading skeleton fill)
- Font: Source Sans 3 (loaded globally)
- text-sm = 15px, text-xs = 13px, text-xl = 20px (project overrides)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PlanCourseCard, ActivityBadge, and PlanSummaryBar components</name>
  <files>src/components/plan/plan-course-card.tsx, src/components/plan/activity-badge.tsx, src/components/plan/plan-summary-bar.tsx</files>
  <read_first>
    - src/lib/plan-types.ts (PlanCourse type for props)
    - src/components/lihp/course-card.tsx (existing card pattern to follow)
    - src/components/lihp/learning-plan-banner.tsx (existing banner styling patterns)
    - .planning/phases/03-plan-generation-display/03-UI-SPEC.md (visual contract)
  </read_first>
  <action>
Create `src/components/plan/` directory.

**1. Create `src/components/plan/activity-badge.tsx`** (per D-11, mocked):

```typescript
// Props: { label: string }
// Renders: inline pill with rounded-md bg-[#f2f5fa] px-2 py-0.5 text-xs text-[#5b6780]
// Small icon left of label (use a generic circle/dot icon from lucide-react or a simple SVG)
// Labels are hardcoded by caller: "Coding exercise", "Role play", "Guided Project", "Capstone Project"
```

Component structure:
- `<span className="inline-flex items-center gap-1 rounded-md bg-[#f2f5fa] px-2 py-0.5 text-xs text-[#5b6780]">`
- Small 12x12 icon (use `<Circle>` from lucide-react with `size={12}`) before label text

**2. Create `src/components/plan/plan-course-card.tsx`** (per D-11, D-12, D-13):

Props: `{ course: PlanCourse }`

Layout (horizontal card):
- Container: `flex items-start gap-4 py-4 px-4` (no outer border -- parent MilestoneSection uses divide-y)
- Left: `<img src={course.imageUrl} alt={course.name} className="h-20 w-20 shrink-0 rounded-lg object-cover bg-[#f2f5fa]" />` (80x80 thumbnail per UI-SPEC)
  - Use `<img>` tag, NOT `next/image` (avoids Amplify image optimization issues per RESEARCH pitfall 6)
  - Add `bg-[#f2f5fa]` as fallback background for broken images
- Right side (flex-1, flex flex-col gap-1):
  - Course name: `<a href={"https://www.coursera.org" + course.url} target="_blank" rel="noopener noreferrer" className="text-sm font-semibold text-[#0f1114] hover:underline">{course.name}</a>` (per D-13, DISP-05)
    - Prepend `https://www.coursera.org` to `course.url` since the URL from Search API is a path like `/professional-certificates/google-data-analytics`
  - Skills line: `<p className="text-xs text-[#5b6780]">{course.skills.slice(0, 4).join(", ")}</p>` (3-4 skills per UI-SPEC)
  - Partner/metadata row: `<p className="text-xs text-[#5b6780]">` containing:
    - Partner name(s): `course.partners.join(", ")`
    - Middot separator: ` · `
    - Product type label: map `course.productType` to display text ("COURSE" -> "Course", "SPECIALIZATION" -> "Specialization", "PROFESSIONAL_CERTIFICATE" -> "Professional Certificate")
    - Middot separator: ` · `
    - Duration: `course.duration`
  - Activity badges row: `<div className="flex flex-wrap gap-1.5 mt-1">` with 1-2 hardcoded `ActivityBadge` components
    - Assign badges based on productType: PROFESSIONAL_CERTIFICATE gets "Capstone Project", SPECIALIZATION gets "Guided Project", COURSE gets "Coding exercise"
    - This is mocked per D-11
- Far right: omit chevron (per D-12, simpler for prototype)

**3. Create `src/components/plan/plan-summary-bar.tsx`** (per DISP-02):

Props: `{ role: string, skills: string[], duration: string, hoursPerWeek: string }`

Single line of text:
```tsx
<div className="text-sm text-[#5b6780]">
  <span className="font-semibold text-[#0f1114]">{role}</span>
  {" · "}
  {skills.slice(0, 5).join(", ")}
  {" · "}
  {duration}
  {" · ~"}
  {hoursPerWeek}
</div>
```
  </action>
  <verify>
    <automated>cd /Users/dajiboye/base/coursera/ppp-prototype && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/plan/activity-badge.tsx contains `export function ActivityBadge`
    - src/components/plan/activity-badge.tsx contains `bg-[#f2f5fa]`
    - src/components/plan/plan-course-card.tsx contains `export function PlanCourseCard`
    - src/components/plan/plan-course-card.tsx contains `target="_blank"`
    - src/components/plan/plan-course-card.tsx contains `coursera.org`
    - src/components/plan/plan-course-card.tsx contains `hover:underline`
    - src/components/plan/plan-course-card.tsx contains `h-20 w-20`
    - src/components/plan/plan-summary-bar.tsx contains `export function PlanSummaryBar`
    - src/components/plan/plan-summary-bar.tsx contains `font-semibold text-[#0f1114]`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Leaf components are ready: PlanCourseCard renders course info with XDP link (DISP-04, DISP-05), ActivityBadge renders mocked activity pills (D-11), PlanSummaryBar shows role/skills/duration summary (DISP-02).</done>
</task>

<task type="auto">
  <name>Task 2: Create MilestoneSection, PlanView, and PlanLoadingSkeleton</name>
  <files>src/components/plan/milestone-section.tsx, src/components/plan/plan-view.tsx, src/components/plan/plan-loading-skeleton.tsx</files>
  <read_first>
    - src/lib/plan-types.ts (PlanMilestone, LearningPlan types)
    - src/components/plan/plan-course-card.tsx (created in Task 1)
    - src/components/plan/plan-summary-bar.tsx (created in Task 1)
    - src/components/lihp/learning-plan-banner.tsx (badge styling pattern to match)
    - .planning/phases/03-plan-generation-display/03-UI-SPEC.md (layout specs)
  </read_first>
  <action>
**1. Create `src/components/plan/milestone-section.tsx`** (per DISP-03, UI-SPEC):

Props: `{ milestone: PlanMilestone }`

Structure:
- Container: `<div className="rounded-lg border border-[#dae1ed] overflow-hidden">`
- Header section: `<div className="px-4 py-3 space-y-1">`
  - Top row (flex justify-between items-center):
    - Milestone name: `<h3 className="text-sm font-semibold text-[#0f1114]">{milestone.name}</h3>`
    - Badges (right side): map `milestone.badges` to `<span className="rounded-md bg-[#e3eeff] px-2 py-0.5 text-xs font-medium text-[#0056d2]">{badge}</span>`
  - Description: `<p className="text-xs text-[#5b6780]">{milestone.description}</p>`
  - Skills line: `<p className="text-xs text-[#5b6780]">Skills: {milestone.skills.join(", ")}</p>`
- Course cards section: `<div className="divide-y divide-[#dae1ed] border-t border-[#dae1ed]">`
  - Map `milestone.courses` to `<PlanCourseCard key={course.id} course={course} />`

**2. Create `src/components/plan/plan-view.tsx`** (per D-09, UI-SPEC layout):

Props: `{ plan: LearningPlan, onBack: () => void }`

Add `"use client";` directive (has click handlers).

Structure:
- Container: `<div className="space-y-6">`
- Back link: `<button onClick={onBack} className="flex items-center gap-1.5 text-xs font-semibold text-[#0056d2] hover:underline">` with `<ArrowLeft size={14} />` from lucide-react + "Back to home"
- Title + CTA row: `<div className="flex items-center justify-between">`
  - Title: `<h1 className="text-xl font-semibold text-[#0f1114]">{plan.title}</h1>`
  - CTA button: `<button className="flex items-center gap-2 rounded-full bg-[#0056d2] px-5 py-2.5 text-sm font-semibold text-white hover:bg-[#0048b0] transition-colors">Start learning plan <ChevronRight size={14} /></button>`
    - Non-functional per Claude's discretion -- no onClick handler needed
- Summary bar: `<PlanSummaryBar role={plan.summary.role} skills={plan.summary.skills} duration={plan.summary.totalDuration} hoursPerWeek={plan.summary.hoursPerWeek} />`
- Milestones: `<div className="space-y-6">` mapping `plan.milestones` to `<MilestoneSection key={milestone.id} milestone={milestone} />`

Import `ArrowLeft` and `ChevronRight` from `lucide-react`.

**3. Create `src/components/plan/plan-loading-skeleton.tsx`** (per D-03):

No props.

Structure mimics PlanView layout with animated placeholders:
- Container: `<div className="space-y-6 animate-pulse">`
- Back link placeholder: `<div className="h-4 w-28 rounded bg-[#f2f5fa]" />`
- Title row: `<div className="flex items-center justify-between"><div className="h-6 w-64 rounded bg-[#f2f5fa]" /><div className="h-10 w-44 rounded-full bg-[#f2f5fa]" /></div>`
- Summary bar: `<div className="h-4 w-96 rounded bg-[#f2f5fa]" />`
- 3 milestone skeletons: each is `<div className="rounded-lg border border-[#dae1ed] p-4 space-y-3">`
  - Header: `<div className="h-4 w-40 rounded bg-[#f2f5fa]" />`
  - Description: `<div className="h-3 w-72 rounded bg-[#f2f5fa]" />`
  - 3 course card skeletons: each `<div className="flex items-start gap-4 py-3"><div className="h-20 w-20 shrink-0 rounded-lg bg-[#f2f5fa]" /><div className="flex-1 space-y-2"><div className="h-4 w-48 rounded bg-[#f2f5fa]" /><div className="h-3 w-32 rounded bg-[#f2f5fa]" /><div className="h-3 w-56 rounded bg-[#f2f5fa]" /></div></div>`
  </action>
  <verify>
    <automated>cd /Users/dajiboye/base/coursera/ppp-prototype && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/plan/milestone-section.tsx contains `export function MilestoneSection`
    - src/components/plan/milestone-section.tsx contains `PlanCourseCard`
    - src/components/plan/milestone-section.tsx contains `divide-y divide-[#dae1ed]`
    - src/components/plan/milestone-section.tsx contains `bg-[#e3eeff]`
    - src/components/plan/plan-view.tsx contains `export function PlanView`
    - src/components/plan/plan-view.tsx contains `ArrowLeft`
    - src/components/plan/plan-view.tsx contains `Back to home`
    - src/components/plan/plan-view.tsx contains `Start learning plan`
    - src/components/plan/plan-view.tsx contains `MilestoneSection`
    - src/components/plan/plan-view.tsx contains `PlanSummaryBar`
    - src/components/plan/plan-loading-skeleton.tsx contains `export function PlanLoadingSkeleton`
    - src/components/plan/plan-loading-skeleton.tsx contains `animate-pulse`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All plan display components are ready: MilestoneSection renders milestone header with badges and course cards (DISP-03), PlanView composes the full plan layout with back link, title, CTA, summary bar, and milestones (DISP-02, DISP-03), PlanLoadingSkeleton shows pulsing placeholders during generation (D-03).</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. All 6 component files exist in `src/components/plan/`
3. PlanCourseCard links to coursera.org with target="_blank" (DISP-05)
4. PlanCourseCard shows name, partner, duration, difficulty, skills (DISP-04)
5. MilestoneSection renders course cards with divide-y dividers (DISP-03)
6. PlanSummaryBar shows role, skills, duration, hours/week (DISP-02)
7. PlanView has "Back to home" link and "Start learning plan" CTA
8. PlanLoadingSkeleton has animate-pulse class
</verification>

<success_criteria>
- All 6 plan display components exist and compile cleanly
- Course cards show all required fields: name, partner, estimated hours, difficulty, skills tags (DISP-04)
- Course names link to coursera.org XDP in new tab (DISP-05)
- Plan summary bar shows target role, key skills, timeline, hours/week (DISP-02)
- Milestone sections contain course cards with proper dividers (DISP-03)
- Loading skeleton provides visual feedback during generation (D-03)
</success_criteria>

<output>
After completion, create `.planning/phases/03-plan-generation-display/03-02-SUMMARY.md`
</output>
